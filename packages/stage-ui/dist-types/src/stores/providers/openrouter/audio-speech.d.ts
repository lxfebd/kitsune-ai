import type { ProviderMetadata } from '../../providers';
export declare function buildOpenRouterAudioSpeechProvider(baseUrlValidator: (baseUrl: unknown) => {
    errors: unknown[];
    reason: string;
    valid: boolean;
} | null | undefined): ProviderMetadata;
