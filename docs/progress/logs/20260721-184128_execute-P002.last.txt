先定位并读取目标文件。
文件已有基本实现。让我检查是否有插件加载系统应该在此处初始化但缺失了。
让我检查 stage-web 的 renderer/main.ts 作为对比参考。
`renderer/main.ts` 已经是一个完整的实现，包含：

1. 所有 Vue 插件的导入和注册（MotionPlugin, autoAnimatePlugin, Pinia, PiniaColada, i18n, Tres）
2. Vue Router 配置（createWebHashHistory + auto-routes）
3. Pinia store 创建
4. App 组件挂载
5. 字体懒加载（requestIdleCallback）
6. CSS 重置和样式导入

插件系统功能在 `App.vue` 中实现，通过 `pluginHostInspectorStore` 管理插件生命周期（list, load, unload, enable, disable），通过 eventa IPC 与主进程通信。这是正确的架构分离——`main.ts` 负责应用引导，`App.vue` 负责运行时初始化。

两个 TODO（vite-plugin-vue-layouts 替换和 autoAnimatePlugin 类型修复）是已知的技术债务，不属于空壳函数。

**P002 已完成** — renderer 入口点功能完整，插件系统正常运行。
