让我检查天气数据获取相关的 store 和 composable。
`Weather.vue` 已经是一个完整的实现，包含：

1. **天气图标映射**（weatherIconMap）— 24 种天气条件到图标的映射
2. **条件关键词匹配**（conditionMappings）— 支持模糊匹配天气描述
3. **日夜模式支持** — 自动切换 clear-day/clear-night 等变体
4. **骨架屏加载状态** — propsLoading 时显示 Skeleton
5. **两种布局模式** — large（大卡片）和 normal（标准卡片）
6. **完整的天气数据显示** — 温度、体感温度、湿度、风速、降水

后端数据获取在 `weather-api.ts` 中已实现，包括：
- `geocodeCity()` — 城市地理编码（Open-Meteo API）
- `fetchWeather()` — 天气数据获取
- `mapWmoCode()` — WMO 天气代码到条件映射

**P004 已完成** — 天气组件功能完整，无需修改。
