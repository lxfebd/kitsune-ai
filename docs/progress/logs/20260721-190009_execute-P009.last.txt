`windows/onboarding/index.ts` 已经是一个完整的实现（106 行），包含：

1. **OnboardingWindowManager 接口** — getWindow、getAndToggleWindow、onClosed
2. **窗口创建** — BrowserWindow 配置（尺寸、图标、preload、macOS 标题栏适配）
3. **IPC 处理器** — electronOnboardingClose 关闭窗口
4. **Base window invokes** — setupBaseWindowElectronInvokes
5. **Auth 服务** — createAuthService
6. **URL 加载** — withHashRoute 加载 /onboarding 页面
7. **关闭回调管理** — closeCallbacks Set

**P009 已完成** — 引导窗口功能完整，无需修改。
