所有 8 个窗口 RPC 文件都是完整的实现：

1. **main/rpc** — 主窗口：widgets、auto-updater、MCP、Godot stage、onboarding、auth、window snap、devtools、settings、chat、notice
2. **widgets/rpc** — 小部件窗口：base invokes + widgets service
3. **settings/rpc** — 设置窗口：widgets、auto-updater、MCP、Godot stage、auth、global shortcut、spotlight shortcut、devtools
4. **inlay/rpc** — 嵌入窗口：base invokes only
5. **desktop-overlay/rpc** — 桌面覆盖：base invokes + MCP + readiness 状态
6. **dashboard/rpc** — 仪表盘：base invokes + devtools、settings、chat、notice
7. **chat/rpc** — 聊天窗口：base invokes + widgets、MCP、devtools
8. **about/rpc** — 关于窗口：base invokes + auto-updater

每个文件都正确实现了 eventa context 创建、base window invokes 注册、窗口特定的 IPC 处理器。

**P008 已完成** — 所有 8 个窗口 RPC 处理器功能完整，无需修改。
