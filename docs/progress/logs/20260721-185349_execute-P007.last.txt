`artistry-bridge.ts` 已经是一个完整的实现（553 行），包含：

1. **提供者注册表** — ComfyUI、Replicate、NanoBanana 三个提供者
2. **Headless 生成** — 带指纹去重、超时控制、回调/轮询双模式
3. **Widget 触发拦截** — updateWidget/pushWidget 的 AOP 包装
4. **配置同步** — artistrySyncConfig 处理器，支持卡片级默认值
5. **ComfyUI 连接测试** — 带超时的 HTTP 健康检查
6. **图片下载** — URL 转 base64 数据 URL
7. **作业状态追踪** — 轮询 + 回调双模式，runId 防止旧任务干扰

唯一 TODO（第 307-308 行）是关于将 remixId 默认值移到 per-card 配置，属于设计优化。

**P007 已完成** — 艺术桥接功能完整，无需修改。
