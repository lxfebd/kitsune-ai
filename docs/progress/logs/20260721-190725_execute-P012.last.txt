`auto-updater.ts` 中有几个空 catch 块需要修复：

1. **第 63 行**：`mkdir` 的 `.catch(() => {})` — 应记录错误
2. **第 64 行**：`appendFile` 的 `.catch(() => {})` — 应记录错误
3. **第 511-513 行**：`subscribe` 中的 catch 块只有注释无错误处理

让我修复这些空 catch 块：
已修复 3 个空 catch 块：

1. **第 63-66 行**：`mkdir` 和 `appendFile` 的 `.catch` 现在记录错误到 console.error
2. **第 513-515 行**：`subscribe` 中的 catch 块现在记录错误到 console.warn

**P012 已完成** — 空 catch 块已修复，错误处理已添加。
