# Default Profile

This is the default configuration profile. It is used when `KITSUNE_PROFILE` environment variable is not set (falls back to `'default'`).

To use a custom profile, set `KITSUNE_PROFILE` to a directory name under `config/`.
